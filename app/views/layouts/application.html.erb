<!DOCTYPE html>
<html>
<head>
  <title>Pinteresting</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <!-- Viewport - allows mobile view -->
  <meta name="viewport" content="width=device-width, intial-scale=1.0">
</head>
<body>
  
  <!-- Header nav bar, rendered from _header.html.erb -->
  <%= render 'layouts/header' %>

  <!-- getbootstrap.com for div class, yield is for stuff in /pages -->
  <div class="container">
      <!-- For flash messages (you are signed in, there is an error, etc.) -->
      <% flash.each do |name, msg| %>
        <%= content_tag(:div, msg, class: "alert alert-info") %>
      <% end %>
    <%= yield %>
  </div>

</body>
</html>
